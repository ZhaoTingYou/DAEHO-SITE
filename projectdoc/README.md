# Project Document Status

The files in this directory preserve earlier design, rollout, and CMS planning
decisions. Some still describe the retired `public/images` and `public/videos`
workflow for historical context.

For current operating instructions, use the repository root `README.md`.
Runtime images and videos now live in object Storage; do not restore the local
static media directories based on an older document in this folder.
