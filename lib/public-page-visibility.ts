/**
 * Temporary public-page switches.
 *
 * Keep the underlying route and CMS content intact so a page can be restored
 * without rebuilding its content or editor configuration.
 */
export const isTechniquePageVisible = true;
